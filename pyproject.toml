[project]
name = "devlens"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    # langchain-google-genai requires langchain-core>=1.6.1 (needs ModelAPIError,
    # added there); pin the floor here so `uv run`'s auto-sync can't drop back
    # to an older, incompatible langchain-core.
    "langchain-core>=1.6.1,<2.0.0",
]
